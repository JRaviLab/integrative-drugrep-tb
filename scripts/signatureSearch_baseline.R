library(signatureSearch)

# get cell line information 
cell_info <- data(cell_info2)
# look for cell lines with cell_type == 'normal'
# get expression data of all the available untreated cell line in LINCS and CMAP drug data base
# subset to get only "landmark genes"
# calculate correlation starting with pearson, spearman etc. 


